# The legacy in-sandbox agent's dependencies (src/harborbox_agent), used only
# when HARBORBOX_RUNTIME_PROVIDER=docker. It is the default value of this
# Dockerfile's SANDBOX_REQUIREMENTS arg, but no template in
# templates/manifest.yaml builds with it -- onvo-pro and onvo-lite both
# override the arg -- and every shipped compose file sets the opensandbox
# provider.
#
# The Jupyter pins below belong to that agent's own kernel (harborbox_agent/
# kernel.py), which is a separate execution path from the one POST
# /v1/executions now takes. They stay until the docker provider itself goes.
fastapi==0.116.1
uvicorn==0.35.0
ipykernel==6.30.1
jupyter-client==8.6.3
